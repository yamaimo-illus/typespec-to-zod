openapi: 3.0.0
info:
  title: (title)
  version: 0.0.0
tags: []
paths: {}
components:
  schemas:
    ExtendedSyntaxForDocDecorator:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: "Username zod: z.string().min(2, { message: 'Must be 2 or more characters long' })"
